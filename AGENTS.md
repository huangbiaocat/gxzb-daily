# 项目级工程规范与执行手册 (招投标网站信息收集)

## 1. 核心流水线与关键入口
- **日历/明细页生产**：`scripts/build_daily_page.py`
- **实机同步与打包部署**：`scripts/pack_and_upload_i5.py`
- **每日主采集入口**：`run_daily.py`、`scripts/collect.py`
- **后台管理系统**：`manager/server.py`、`web/`

## 2. 反碎步与防死循环约束 (硬性执行)
- **严禁编写临时探测脚本**：严禁用 `python3 -c "with open(...) ..."` 或反复 `cat`/`sed`/`awk` 切片探查代码库。
- **强制精准定位**：查找代码必须直接使用 `rg -n "关键词"` 精确命中文件与行号。
- **编辑一次到位**：编辑代码优先使用 `apply_patch` 或脚本全量重写，严禁边看边探、碎步试错。
- **5步只读熔断**：单轮执行中，连续纯只读命令（`ls`, `cat`, `rg`, `git log`, `git diff`）达到 5 次且未发生文件修改时，必须强制刹车并落地。

## 3. 任务拆分原则
- 复合任务（如：写采集脚本 + 修改管理端前端 + 部署到生产服务器 + Git 提交）必须严格分步骤推进，禁止跨模块多文件反复横跳。
- 独立采集脚本必须先在独立单测中闭环验证，严禁将大量调试输出注入主交互线程。
